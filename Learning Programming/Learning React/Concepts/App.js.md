Basic Requirements:

```jsx
import './App.css';


function App() { // or const App = () => {...}
  return (
    <p>this is where content and JSX elements go</p>
  );
}

export default App;
```

>[!export default App;]
>Exports this component to index.js to be rendered into the DOM
>**Is vital in all components that are called in index.js**

![[Pasted image 20240102020930.png]]