>[!What is react?]
>An efficient JavaScript library that updates DOM in real-time by identifying interacted with, and changed components on-screen. 

1. Elements are created using JSX; JSX is an HTML5 copy with a few syntax differences

