
What I Did:

1. Initialized a client and server file

2. Used "npx create-[[React]]-app ." in the client directory using the terminal (Used "cd" function to navigate amongst folder)

3. Used "[[Yarn]] init" in the server folder directory

4. Created a JavaScript file in the "server" directory to host my server code

5. Used "[[Yarn]] start" in the server directory to keep it running so that I don't have to save and relaunch the server every time

6. In the [[Package.json]] in the server folder I added the "scripts: {start: "[[Nodemon]] [[Index.js]]"},"

7. Started the site with npm start

**What I learned:**

1. How to initialize a [[React]] app

2. Install necessary packages such as in the server folder ("[[Yarn]] add [[Express]] [[Cors]] [[Nodemon]] socket.io")

3. You can never send data from client to client, only from server first, then client after
4. For the basic outline check out [[React Application Outline]], all of which is already given with:
```typescript
npx create-react-app .
```

>It's just a matter of what you delete
  




 


