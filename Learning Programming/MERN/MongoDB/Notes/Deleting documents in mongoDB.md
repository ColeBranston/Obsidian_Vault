Deleting one document:
`db.collection.deleteOne({name:"Larry"})`
Deletes anyone with the name of Larry

Deleting multiple documents:
`db.collection.deleteMany({fullTime:false})`
Deletes anyone with a fullTime field set to false.

