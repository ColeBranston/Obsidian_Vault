Review the [[HTTP Methods]]

The syntax looks something like this:

``` javascript
app.post(path, (req, res)=>{
	// Functionality goes here
})
```
The path is the actual path (like ./api/path/directory) and is a string.

