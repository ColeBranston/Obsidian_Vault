A static asset is a file that the server does not have to change. We dump all our assets in one folder and allow express to set them up using the "static()" method. Some static assets are: image files, styling files, and javascript files. 

