API means setting up an [[HTTP]] interface. Data is sent using JSON (JavaScript Object Notation), and to send back a response, we use res.json() method.

SSR (server side rendering) allows us to set up templates, and send back entire HTML, CSS and JS, using the res.render() method.

