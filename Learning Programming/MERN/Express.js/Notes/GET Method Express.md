Review the [[HTTP Methods]]

```javascript
app.get(path, (request, response)=>{
	function;
})
```

The path is the api link that you want.

The request and response parameters represent the USER request and the response we send back.

Then in the callback function we implement whatever functionality we want.
