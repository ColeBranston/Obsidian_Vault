NodeJS does not have access to browser APIs. Unlike browser apps, with Node, we build server side apps. Node apps consist of pure logic without a graphical interface. 

NodeJS however, does have a filesystem, is based on the NodeJS version it was built in (unlike browsers which are based on the user's browser), and you have access to CommonJS modules by default.