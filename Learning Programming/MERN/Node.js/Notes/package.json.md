a manifest file that stores important info about the project.package. There are three ways we can make package.json

1. Manually, creating it within the root
2. npm init (step by step, press enter to skip)
3. npm init -y (everything default)
