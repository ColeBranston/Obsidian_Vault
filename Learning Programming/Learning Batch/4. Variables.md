
- Can create variables with `set`

```bat
set varname="Howdy"
```

**Note:** no spaces between the varname=howdy (If there are spaces the set operator breaks)

- varname is now stored -> assigned to "Howdy"

- To then call that same variable again after its been assigned use `echo` and `%varname%`

```bat
echo %varname%
```

