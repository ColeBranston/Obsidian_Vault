A programming language that's divided into classes. Usually has a "main" -driver method that runs everything. 

All you code exists in methods, functions, and classes that are encompassed by a "master class" which is basically just the program. Review java if this doesn't  make sense