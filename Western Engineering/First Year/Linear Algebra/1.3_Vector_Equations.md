$R^2$ ; a vector in $R^2$ has two components
$\hat{u}$ = $[u_1, u_2]$ 

If $\hat{v}$ = $[v_1,v_2]$, $\hat u$ = $\hat v$ <=> $u_1 = v_1, u_2 = v_2$

Operations:
$\hat u + \hat v$ = $[u_1 + v_1, u_2 + v_2]$
$a * \hat u = [au_1, au_2]$
![[Pasted image 20240115110942.png]]
Note: $\hat u$, and $c*\hat u$ are parallel 

$R^3$ has three components: $\hat w = [w_1,w_1,w_3]$
$R^n$ has n components: $\hat w = [w_1, w_2, ... , w_n]$
$\hat 0 = [0, 0, ... , 0]$ is the 0 vector

### Linear Combinations
$\hat v_1, \hat v_2, ... , \hat v_n$ in $R^n$
$c_1, c_2, ... , c_n$ scalars (Numbers in R)

$\hat y = c_1 \hat v_1 + c_2 \hat v_2 + ... + c_m \hat v_m$
is the linear combination of $\hat v_1, \hat v_2, ... , \hat v_m$

Theorem: $x_1 \hat a_1 + x_2 \hat a_2 + ... + x_r \hat a_r = \hat b$
has the same solution set (is equivalent) to the system with augmented matrix

$[\hat a_1, \hat a_2 ... \hat a_r \hat b]$ $\hat a_1, a_2$ columns

Definition:

$\hat v_1, \hat v_2, ... , \hat v_r$ vectors in $R^n$ The set of all linear combinations of $\hat v_1, \hat v_2, ... ,  \hat v_r$ is called the subset of $R^n$ spanned by $\hat v_1, \hat v_2, ... ,  \hat v_r$

span{$\hat v_1, \hat v_2, ... , \hat v_r$}

When does b$\epsilon$span{$\hat v_1, ... \hat v_r$}
is the same as:
can b be written as a linear combination of $\hat v_1, \hat v_2, ... ,  \hat v_r$
