**Generalization** is the process of *minimizing the differences* between entities by identifying their common characteristics — a bottom-up way of pulling shared attributes out of several entities into one [[Superclass]].

- The reverse direction of [[Specialization]].
- e.g. `Staff`, `PrivateOwner`, and `Client` all have a number and a name, so they can be generalized into `Person`.
