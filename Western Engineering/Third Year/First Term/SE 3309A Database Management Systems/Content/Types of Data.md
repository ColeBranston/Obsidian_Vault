Data falls into three categories:

- **Structured** — organizes cleanly into tables and rows (numbers, dates)
- **Semi-structured** — some structure but flexible (XML, JSON)
- **Unstructured** — doesn't fit tables (images, audio, video)

This course's focus: **structured data**.
