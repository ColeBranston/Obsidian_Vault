A **view** lets each user have their own perspective of a [[Database]] — essentially some subset of the database, built by a query over one or more tables.

**Benefits:**
- Reduces complexity
- Provides a level of security
- Customizes the appearance of the database per user
- Presents a consistent, unchanging picture of the structure even if the underlying database is changed
