**Transition requirements** describe how the solution will be deployed and released into production — e.g. migration and training requirements.

*Example:* the system must be deployed as a Docker container.

They describe the temporary capabilities needed to migrate from the current state to the future state, including converting data from the current system and training to close skill gaps.
