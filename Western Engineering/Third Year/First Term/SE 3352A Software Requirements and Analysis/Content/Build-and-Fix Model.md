The earliest development **"model"** — really just an ad hoc loop, with no planning or design phase:

**Build → Use → Fix → Use → … → Retirement**

You build something, use it, fix what breaks, and repeat until the system is retired.
