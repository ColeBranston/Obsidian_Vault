The **network edge** is where applications run: the [[Host]]s that originate and consume data.

They never carry traffic for anyone else. Everything they send reaches the [[Network Core]] through an [[Access Network]].
