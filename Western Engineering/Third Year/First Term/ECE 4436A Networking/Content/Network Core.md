The **network core** is the meshed set of [[Router]]s that forwards packets between the networks at the edge.

It runs none of your programs and holds none of your data — it only forwards. "Meshed" means many paths exist between any two routers, which is what lets [[Routing]] reroute around a failed link.
