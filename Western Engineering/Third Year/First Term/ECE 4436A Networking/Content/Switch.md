A **switch** joins devices inside **ONE** [[Network]], forwarding by hardware address — the [[MAC Address]].

Forwarding decision: **MAC**. It never opens anything above the link layer (see [[Protocol Layering]]), which is why a switch can be cheap and fast.
