- The Arrays class contains various static methods for sorting and searching arrays, for comparing arrays, and for filling array with data-elements.
- It also contains a static method for converting an array to a list. (Array.asList())

### Arrays Class UML Diagram

![[Pasted image 20241011125422.png]]

The following code example creates an array of int values, fills part of the array with a value of 50, sorts it, searches for an element, and compares the array with another.

![[Pasted image 20241011125540.png]]