$$\overset{->}{A} \cdot \overset{->}{B} = |\overset{->}{A}||\overset{->}{B}|cos\phi$$
$$= A_x \cdot B_x + A_y\cdot B_y \text{    (Gives a Scalar)}$$

Used to get angle between two vectors -> Gives a scalar value