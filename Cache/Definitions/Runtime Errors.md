- or Execution error, known as Exception in Java, occur
when the programming environment detects an operation that is impossible to carry out during program execution (when the program is running).
- Java provides options to catch Exception, and if needed, terminate the program gracefully. Example of a runtime error would be “an integer divided by an integer zero”.