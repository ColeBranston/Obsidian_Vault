
## Lists

- Similar to normal array but have different method of declaring them.

Example:

	List<Integer> listName = new ArrayList<Integer>;

- Basically python lists ie. java arrays that store data types but can now increase and decrease with size.